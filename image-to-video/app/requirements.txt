# 핵심 앱(서버·목업)은 표준 라이브러리만으로 동작 — 설치 없이 실행 가능.
# 아래는 '실제 생성'을 켤 때만 필요한 선택 의존성.

# [PLAN] Claude 스토리 기획 (ANTHROPIC_API_KEY 사용 시)
anthropic>=0.40.0

# [IMAGE 목업] 플레이스홀더 이미지 생성 (보통 기본 제공). 없으면: pip install pillow
pillow>=10.0.0

# 참고: ffmpeg / ffprobe 는 시스템에 설치되어 있어야 함(영상 목업/조립).
